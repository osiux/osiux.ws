---
import { Image } from 'astro:assets';

interface Props {
	flickrLink: string;
}

const { flickrLink } = Astro.props;
---

<Image
	src={`https://live.staticflickr.com/${flickrLink}`}
	alt={flickrLink}
	inferSize
/>
